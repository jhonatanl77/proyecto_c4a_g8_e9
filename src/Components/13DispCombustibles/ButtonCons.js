import React from 'react';
import { Link } from "react-router-dom";
import {Button, Modal,Form, Container, Row,Col} from 'react-bootstrap';
function ButtonCons(){
    return(
            <Button variant="primary" type="submit">
                Consultar
            </Button>
    )

};
export default ButtonCons;